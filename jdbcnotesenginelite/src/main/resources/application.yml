engine:
  defaults:
    book: 2
    author: 2

spring:
  datasource:
    spring:
      datasource: url:jdbc:h2:mem:testdb
      initialization-mode:  always
  shell:
    interactive:
      enabled: true

logging:
  level:
    ROOT: INFO

server:
  port: 8081